<div>
    <a href="<?php echo $this->url(
                                array(
                                    'action' => 'add',
                                    'controller' => 'news-tags'
                                ),
                                'admin', true); ?>">Dodaj oznaku</a>
</div>

<?php if(count($this->tags) > 0): ?>
<table>
    <thead>
        <tr>
            <th>Naziv</th>
            <th>Slug</th>
            <th colspan="2">Akcija</th>
        </tr>
    </thead>
    <?php foreach($this->tags as $tag): ?>
    <tr style="background-color:<?php echo $this->cycle(array("#FFFFFF","#F1F1F1"))->next()?>">
        <td>
            <?php echo $this->escape($tag->title); ?>
        </td>

        <td>
            <?php echo $this->escape($tag->slug); ?>
        </td>

        <td>
            <a href="<?php echo $this->url(array(
                                            'action' => 'edit',
                                            'controller' => 'news-tags',
                                            'id' => $tag->id
                                        ),
                                        'admin', true); ?>">Izmeni</a>
        </td>
        <td>
            <a class="delete" href="<?php echo $this->url(array(
                                            'action' => 'delete',
                                            'controller' => 'news-tags',
                                            'id' => $tag->id
                                        ),
                                        'admin', true); ?>">Obriši</a>
        </td>
    </tr>
    <?php endforeach; ?>
</table>

<?php echo $this->paginationControl($this->tags, 'Sliding', 'paginator.phtml'); ?>

<?php else: ?>
Nema oznaka vesti
<?php endif; ?>